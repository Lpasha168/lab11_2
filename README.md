# Лабораторная работа №11 — Облако точек (вариант 3)

Qt-приложение для загрузки/генерации облака точек, преобразований матрицами 4×4, статистического анализа, гистограмм и SOR-фильтрации с сравнением последовательной и параллельной обработки.

## Алгоритмы

### Преобразования (матрицы 4×4, ЛР №10)

Однородные координаты: `[x', y', z', 1]ᵀ = M · [x, y, z, 1]ᵀ`.

- **Смещение** `T(tx, ty, tz)`
- **Повороты** `Rx`, `Ry`, `Rz` вокруг осей
- **Композиция**: накопительная матрица `M_total *= T · Rz · Ry · Rx` при каждом нажатии «Применить»

### SOR (Statistical Outlier Removal)

1. Для каждой точки вычисляется среднее расстояние до **K** ближайших соседей.
2. По всем таким средним считаются глобальные **μ** и **σ**.
3. Порог: `τ = μ + k_σ · σ`. Точки с `meanDist > τ` помечаются как выбросы (`removed = true`).

### Параллелизация

- **Последовательно**: цикл по точкам.
- **Параллельно**: `std::async(std::launch::async)` для каждой точки.
- **Speedup** = `T_seq / T_par` (замер `std::chrono::steady_clock`).

### Статистика

По осям X, Y, Z и по массиву средних расстояний до соседей: min, max, среднее, σ. После SOR — только по **inliers** (не удалённым точкам).

## Сборка и запуск

### Требования

- Qt 6 (модуль Widgets) или Qt 5.15+
- Компилятор с поддержкой C++17 (MSVC, MinGW, GCC, Clang)

### Qt Creator

1. Открыть `lab.pro`
2. Выбрать kit (Desktop Qt)
3. Собрать и запустить

### Командная строка

```bash
qmake lab.pro
make          # или nmake / mingw32-make на Windows
./lab         # или lab.exe
```

## Использование

1. **Сгенерировать с шумом** — 2000 точек в кубе + 150 выбросов.
2. **Загрузить XYZ** — текстовый файл `x y z` построчно.
3. Задать **Tx, Ty, Tz, Rx, Ry, Rz** и нажать **Применить** (композиция матриц).
4. Вкладка **Гистограммы** — распределение X, Y, Z (inliers).
5. **SOR**: Mean K, Stddev Mul → **Применить SOR** — красные выбросы, зелёные inliers, время и speedup.
6. Таблица справа обновляется автоматически.

## Таблица Speedup (заполнить замерами)

Замеры на вашей машине: SOR, Mean K=20, Stddev Mul=1.0.

| Точек (N) | T_seq (мс) | T_par (мс) | Speedup |
|-----------|------------|------------|---------|
| 2 150     |            |            |         |
| 5 000     |            |            |         |
| 10 000    |            |            |         |

*Для больших N сгенерируйте облако программно или загрузите файл.*

## Скриншоты

Добавьте в репозиторий папку `screenshots/`:

- `cloud.png` — визуализация облака
- `histograms.png` — гистограммы X/Y/Z
- `sor.png` — результат фильтрации
- `stats.png` — таблица статистики и speedup

## Структура проекта

```
lab.pro
main.cpp
mainwindow.h / .cpp    — GUI
helpers.h / .cpp       — точка, отрисовка, статистика
matrix.h / .cpp        — матрицы 4×4 (ЛР №10)
sorfilter.h / .cpp     — SOR seq/par + тайминг
histogramwidget.h/.cpp — гистограммы QPainter
io_xyz.h / .cpp        — загрузка XYZ
```

## GitHub

Разместите репозиторий на GitHub и укажите ссылку в отчёте. README должно совпадать с фактическим поведением приложения.
